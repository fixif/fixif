{# comment #}
	%FPF={{FPF.Qnotation()}}
{# unsigned representation #}
{% if not FPF._signed %}	
	{% for x in range(-FPF._alpha,-min(FPF._gamma,0)) %}
	\draw ({{x}},{{y_origin}}) rectangle ++(1,1) [alpha] node[midway] {};
	{% endofr %}
	{% for x in range(-FPF._alpha,0) %}
	\draw ({{x}},{{y_origin}}) rectangle ++(1,1) [alpha] node[midway] {};
	{% endofr %}
	
{% endif %}


	\draw ({{-FPF.alpha}},{{y_origin}}) rectangle ++(1,1) [%s] node[midway] {%s};\n"%( , , 'sign' if self._signed else 'alphas', 's' if self._signed else '')
		# other bits
		for a in range(self.alpha-1,0,-1):
			st += "\t\\draw (%f,%f) rectangle ++(1,1) [alphas] node[midway] {};\n"%(-a,y_origin)
		for g in range(self.gamma):
			st += "\t\\draw (%f,%f) rectangle ++(1,1) [gammas] node[midway] {};\n"%(g,y_origin)
{# binary-point position #}
{% if binary_point: %}
	\draw[black,fill] (0,0) circle [radius=0.1cm];
{% endfor %}